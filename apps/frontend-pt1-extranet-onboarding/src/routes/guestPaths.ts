/** Canonical guest welcome / Wegwijzer route (see `App.tsx`). */
export const PUBLIC_GUEST_WELCOME_PATH = "/welcome";

/** Canonical guest sign-in route (see `App.tsx`). */
export const PUBLIC_GUEST_LOGIN_PATH = "/login";
