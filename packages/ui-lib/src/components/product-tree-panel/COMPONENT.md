# ProductTreePanel

Card + optional filter + **group / product** tree. Groups are collapsible; product rows call `onSelectProduct`. Use `expandedIds` + `onToggle` for controlled expansion (no client graph engine here — Task B provides nodes).
