# TimelineEventMarker

Scaffolded by `scaffold workspace-ui add-component` (template variant **`minimal`** — Card + optional footer action).

## Checklist (AI / humans)

1. **Presentation** — Keep `TimelineEventMarker.tsx` dumb; import primitives from `@scope/ui` (root entry).
2. **Stories** — Add variants (loading, empty, error) as needed.
3. **State** — Add a `useTimelineEventMarker` hook in this folder if you need state, then export it from `index.ts`.
4. **Exports** — This folder's `index.ts` and `packages/ui-lib/src/index.ts` are updated by the scaffold.
5. **Primitives** — Import from `@scope/ui` (root); do not run `shadcn add` here.
