# DensityToggle

Scaffolded by `scaffold workspace-ui add-component` (template variant **`settings`** — Card + Switch rows).

## Checklist (AI / humans)

1. **Presentation** — `DensityToggle.tsx` only imports primitives from `@scope/ui`. No data fetching.
2. **State** — `items` are fully controlled; Add a `useDensityToggle` hook in this folder if you need state, then export it from `index.ts`.
3. **Composition** — For dense tables, prefer variant **`data-panel`**; for single form columns, **`form-field`**.
4. **Exports** — `index.ts` and `packages/ui-lib/src/index.ts` are updated by the scaffold.
